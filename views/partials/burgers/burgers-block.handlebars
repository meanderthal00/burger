<tr>
    <td>
        {{burger_name}}
    </td>
    <td>
        <button type="button" class="btn btn-info" id="devourMe" data-id="{{id}}" data-newdevoured="{{devoured}}">
            Take all the bites
        </button>

    </td>
</tr>